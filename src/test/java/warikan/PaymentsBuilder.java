package warikan;

import warikan.domain.PaymentKubun;
import warikan.domain.Payments;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class PaymentsBuilder {
//    public static Payments create(List<String> memberNames, List<PaymentKubun> paymentKubuns, int pricePerWeight){
//        int i = 0;
//        return new Payments(
//        [].stream()
//                .map(memberName -> {return PaymentBuilder.create(memberName,paymentKubuns[i],pricePerWeight);})
//                .collect(Collectors.toList())
//        );
//    }

//    public static Payments create(List<String[]> values){
//        int i = 0;
//        return new Payments(
//                values.stream()
//                        .map(value -> {return PaymentBuilder.create(memberName,paymentKubuns.get(i),pricePerWeight);})
//                        .collect(Collectors.toList())
//        );
//    }

}
